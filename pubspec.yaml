name: fuel_checker
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '^3.5.0' # Updated SDK constraint

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0 # Added provider dependency
  geolocator: ^13.0.2 # Updated to the latest version
  email_validator: ^3.0.0 # Updated to the latest version
  mailer: ^6.3.0 # Updated to the latest version
  flutter_map: ^7.0.2 # Updated to the latest version
  google_maps_flutter: ^2.0.0 # Add any other missing dependencies here
  location_web: ^5.0.4 # Updated to the latest version

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/map_background.png
    - lib/assets/images/logo-full-color-150-x-1.png
    - lib/assets/images/Favourites.png
    - lib/assets/images/Trends.png
    - lib/assets/images/nearby.png
    - lib/assets/images/my trips.png
    - lib/assets/images/Settings.png
    - lib/assets/images/apple icon logo.png
    - lib/assets/images/Black Google Icon.png
    - lib/assets/images/Facebook Icon.png
