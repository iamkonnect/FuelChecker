name: fuel_checker
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.9.0 # Check for the latest version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/arrow-left.png
    - assets/icon_station.png
    - assets/icon_trends.png
    - assets/icon_my_trip.png
    - assets/icon_nearby.png
    - assets/icon_favourite.png
    - assets/icon_settings.png
    - assets/map_placeholder.jpg