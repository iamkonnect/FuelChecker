specVersion: "v1beta"
serviceId: "NewFuelChecker"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "new_database"
      cloudSql:
        instanceId: "FuelChecker-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
